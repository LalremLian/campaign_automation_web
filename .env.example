# PostgreSQL connection string (required for database operations)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/campaign_automation

# API server port (default: 8080)
PORT=8080

# Marketing dashboard port (default: 5173)
# PORT=5173

# API URL used by the dashboard dev server proxy (default: http://localhost:8080)
API_URL=http://localhost:8080
